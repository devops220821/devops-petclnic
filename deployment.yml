apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-petclinic1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring-petclinic1
  template:
    metadata:
      labels:
        app: spring-petclinic1
    spec:
      containers:
      - name: spring-petclinic1
        image: rathaiah/spring-petclinic:latest
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic1
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 32000
  selector:
    app: spring-petclinic1
