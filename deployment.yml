apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-petclinic-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring-petclinic-demo
  template:
    metadata:
      labels:
        app: spring-petclinic-demo
    spec:
      containers:
      - name: spring-petclinic-demo
        image: rathaiah/spring-petclinic:2
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic-demo
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 32000
  selector:
    app: spring-petclinic-demo
